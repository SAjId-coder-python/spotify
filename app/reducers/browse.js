/* Reducer de layout */
import * as types from '../constants/actionTypes';

const INITIAL_STATE = {
}

// exporta o state
export default function(state = INITIAL_STATE, action) {

  switch(action.type) {

    default:
      return state;
  }
}
